---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: clusters.mrs.flexibleengine.upbound.io
spec:
  group: mrs.flexibleengine.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - flexibleengine
    kind: Cluster
    listKind: ClusterList
    plural: clusters
    singular: cluster
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: 'Cluster is the Schema for the Clusters API. ""page_title: "flexibleengine_mrs_cluster_v2"'
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ClusterSpec defines the desired state of Cluster
            properties:
              deletionPolicy:
                default: Delete
                description: DeletionPolicy specifies what will happen to the underlying
                  external when this managed resource is deleted - either "Delete"
                  or "Orphan" the external resource.
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  analysisCoreNodes:
                    description: Specifies a list of the informations about the analysis
                      core nodes in the MRS cluster. The nodes object structure of
                      the analysis_core_nodes is documented below.
                    items:
                      properties:
                        assignedRoles:
                          description: Specifies the roles deployed in a node group.
                            This argument is mandatory when the cluster type is CUSTOM.
                            Each character string represents a role expression.
                          items:
                            type: string
                          type: array
                        dataVolumeCount:
                          description: 'Specifies the data disk number of the nodes.
                            The number configuration of each node are as follows:'
                          type: number
                        dataVolumeSize:
                          description: Specifies the data disk size of the nodes,in
                            GB. The value range is 10 to 32768. Required if data_volume_count
                            is greater than zero. Changing this will create a new
                            MRS cluster resource.
                          type: number
                        dataVolumeType:
                          description: 'Specifies the data disk flavor of the nodes.
                            Required if data_volume_count is greater than zero. Changing
                            this will create a new MRS cluster resource. The following
                            disk types are supported:'
                          type: string
                        flavor:
                          description: Specifies the instance specifications for each
                            nodes in node group. Changing this will create a new MRS
                            cluster resource.
                          type: string
                        nodeNumber:
                          description: Specifies the number of nodes for the node
                            group. Only the core group and task group updations are
                            allowed. The number of nodes after scaling cannot be less
                            than the number of nodes originally created.
                          type: number
                        rootVolumeSize:
                          description: Specifies the system disk size of the nodes.
                            Changing this will create a new MRS cluster resource.
                          type: number
                        rootVolumeType:
                          description: Specifies the system disk flavor of the nodes.
                            Changing this will create a new MRS cluster resource.
                          type: string
                      required:
                      - dataVolumeCount
                      - flavor
                      - nodeNumber
                      - rootVolumeSize
                      - rootVolumeType
                      type: object
                    type: array
                  analysisTaskNodes:
                    description: Specifies a list of the informations about the analysis
                      task nodes in the MRS cluster. The nodes object structure of
                      the analysis_task_nodes is documented below.
                    items:
                      properties:
                        assignedRoles:
                          description: Specifies the roles deployed in a node group.
                            This argument is mandatory when the cluster type is CUSTOM.
                            Each character string represents a role expression.
                          items:
                            type: string
                          type: array
                        dataVolumeCount:
                          description: 'Specifies the data disk number of the nodes.
                            The number configuration of each node are as follows:'
                          type: number
                        dataVolumeSize:
                          description: Specifies the data disk size of the nodes,in
                            GB. The value range is 10 to 32768. Required if data_volume_count
                            is greater than zero. Changing this will create a new
                            MRS cluster resource.
                          type: number
                        dataVolumeType:
                          description: 'Specifies the data disk flavor of the nodes.
                            Required if data_volume_count is greater than zero. Changing
                            this will create a new MRS cluster resource. The following
                            disk types are supported:'
                          type: string
                        flavor:
                          description: Specifies the instance specifications for each
                            nodes in node group. Changing this will create a new MRS
                            cluster resource.
                          type: string
                        nodeNumber:
                          description: Specifies the number of nodes for the node
                            group. Only the core group and task group updations are
                            allowed. The number of nodes after scaling cannot be less
                            than the number of nodes originally created.
                          type: number
                        rootVolumeSize:
                          description: Specifies the system disk size of the nodes.
                            Changing this will create a new MRS cluster resource.
                          type: number
                        rootVolumeType:
                          description: Specifies the system disk flavor of the nodes.
                            Changing this will create a new MRS cluster resource.
                          type: string
                      required:
                      - dataVolumeCount
                      - flavor
                      - nodeNumber
                      - rootVolumeSize
                      - rootVolumeType
                      type: object
                    type: array
                  availabilityZone:
                    description: Specifies the availability zone in which to create
                      the cluster. Changing this will create a new MRS cluster resource.
                    type: string
                  componentList:
                    description: 'Specifies the list of component names. Changing
                      this will create a new MRS cluster resource. The supported components
                      are as follows:'
                    items:
                      type: string
                    type: array
                  customNodes:
                    description: Specifies a list of the informations about the custom
                      nodes in the MRS cluster. The nodes object structure of the
                      custom_nodes is documented below. Unlike other nodes, it needs
                      to specify group_name.
                    items:
                      properties:
                        assignedRoles:
                          description: Specifies the roles deployed in a node group.
                            This argument is mandatory when the cluster type is CUSTOM.
                            Each character string represents a role expression.
                          items:
                            type: string
                          type: array
                        dataVolumeCount:
                          description: 'Specifies the data disk number of the nodes.
                            The number configuration of each node are as follows:'
                          type: number
                        dataVolumeSize:
                          description: Specifies the data disk size of the nodes,in
                            GB. The value range is 10 to 32768. Required if data_volume_count
                            is greater than zero. Changing this will create a new
                            MRS cluster resource.
                          type: number
                        dataVolumeType:
                          description: 'Specifies the data disk flavor of the nodes.
                            Required if data_volume_count is greater than zero. Changing
                            this will create a new MRS cluster resource. The following
                            disk types are supported:'
                          type: string
                        flavor:
                          description: Specifies the instance specifications for each
                            nodes in node group. Changing this will create a new MRS
                            cluster resource.
                          type: string
                        groupName:
                          description: Specifies the name of nodes for the node group.
                            This argument is mandatory when the cluster type is CUSTOM.
                          type: string
                        nodeNumber:
                          description: Specifies the number of nodes for the node
                            group. Only the core group and task group updations are
                            allowed. The number of nodes after scaling cannot be less
                            than the number of nodes originally created.
                          type: number
                        rootVolumeSize:
                          description: Specifies the system disk size of the nodes.
                            Changing this will create a new MRS cluster resource.
                          type: number
                        rootVolumeType:
                          description: Specifies the system disk flavor of the nodes.
                            Changing this will create a new MRS cluster resource.
                          type: string
                      required:
                      - dataVolumeCount
                      - flavor
                      - groupName
                      - nodeNumber
                      - rootVolumeSize
                      - rootVolumeType
                      type: object
                    type: array
                  eipId:
                    description: Specifies the EIP ID which bound to the MRS cluster.
                      The EIP must have been created and must be in the same region
                      as the cluster. Changing this will create a new MRS cluster
                      resource.
                    type: string
                  eipIdRef:
                    description: Reference to a EIP in eip to populate eipId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  eipIdSelector:
                    description: Selector for a EIP in eip to populate eipId.
                    properties:
                      matchControllerRef:
                        description: MatchControllerRef ensures an object with the
                          same controller reference as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  logCollection:
                    description: Specifies whether logs are collected when cluster
                      installation fails. Default to true. If log_collection set true,
                      the OBS buckets will be created and only used to collect logs
                      that record MRS cluster creation failures. Changing this will
                      create a new MRS cluster resource.
                    type: boolean
                  managerAdminPwdSecretRef:
                    description: 'Specifies the administrator password, which is used
                      to login to the cluster management page. The password can contain
                      8 to 26 charactors and cannot be the username or the username
                      spelled backwards. The password must contain lowercase letters,
                      uppercase letters, digits, spaces and the special characters:
                      !?,.:-_{}[]@$^+=/. Changing this will create a new MRS cluster
                      resource.'
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  masterNodes:
                    description: Specifies a list of the informations about the master
                      nodes in the MRS cluster. The nodes object structure of the
                      master_nodes is documented below. Changing this will create
                      a new MRS cluster resource.
                    items:
                      properties:
                        assignedRoles:
                          description: Specifies the roles deployed in a node group.
                            This argument is mandatory when the cluster type is CUSTOM.
                            Each character string represents a role expression.
                          items:
                            type: string
                          type: array
                        dataVolumeCount:
                          description: 'Specifies the data disk number of the nodes.
                            The number configuration of each node are as follows:'
                          type: number
                        dataVolumeSize:
                          description: Specifies the data disk size of the nodes,in
                            GB. The value range is 10 to 32768. Required if data_volume_count
                            is greater than zero. Changing this will create a new
                            MRS cluster resource.
                          type: number
                        dataVolumeType:
                          description: 'Specifies the data disk flavor of the nodes.
                            Required if data_volume_count is greater than zero. Changing
                            this will create a new MRS cluster resource. The following
                            disk types are supported:'
                          type: string
                        flavor:
                          description: Specifies the instance specifications for each
                            nodes in node group. Changing this will create a new MRS
                            cluster resource.
                          type: string
                        nodeNumber:
                          description: Specifies the number of nodes for the node
                            group. Only the core group and task group updations are
                            allowed. The number of nodes after scaling cannot be less
                            than the number of nodes originally created.
                          type: number
                        rootVolumeSize:
                          description: Specifies the system disk size of the nodes.
                            Changing this will create a new MRS cluster resource.
                          type: number
                        rootVolumeType:
                          description: Specifies the system disk flavor of the nodes.
                            Changing this will create a new MRS cluster resource.
                          type: string
                      required:
                      - dataVolumeCount
                      - flavor
                      - nodeNumber
                      - rootVolumeSize
                      - rootVolumeType
                      type: object
                    type: array
                  name:
                    description: Specifies the name of the MRS cluster. The name can
                      contain 2 to 64 characters, which may consist of letters, digits,
                      underscores (_) and hyphens (-). Changing this will create a
                      new MRS cluster resource.
                    type: string
                  nodeKeyPairSecretRef:
                    description: Specifies the name of a key pair, which is used to
                      login to the each nodes(ECSs). Changing this will create a new
                      MRS cluster resource.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  publicIp:
                    description: Specifies the EIP address which bound to the MRS
                      cluster. The EIP must have been created and must be in the same
                      region as the cluster. Changing this will create a new MRS cluster
                      resource.
                    type: string
                  region:
                    description: The region in which to create the MRS cluster resource.
                      If omitted, the provider-level region will be used. Changing
                      this will create a new MRS cluster resource.
                    type: string
                  safeMode:
                    description: Specifies whether the running mode of the MRS cluster
                      is secure, default to true.
                    type: boolean
                  securityGroupIds:
                    description: Specifies an array of one or more security group
                      ID to attach to the MRS cluster. If using the specified security
                      group, the group need to open the specified port (9022) rules.
                    items:
                      type: string
                    type: array
                  streamingCoreNodes:
                    description: Specifies a list of the informations about the streaming
                      core nodes in the MRS cluster. The nodes object structure of
                      the streaming_core_nodes is documented below.
                    items:
                      properties:
                        assignedRoles:
                          description: Specifies the roles deployed in a node group.
                            This argument is mandatory when the cluster type is CUSTOM.
                            Each character string represents a role expression.
                          items:
                            type: string
                          type: array
                        dataVolumeCount:
                          description: 'Specifies the data disk number of the nodes.
                            The number configuration of each node are as follows:'
                          type: number
                        dataVolumeSize:
                          description: Specifies the data disk size of the nodes,in
                            GB. The value range is 10 to 32768. Required if data_volume_count
                            is greater than zero. Changing this will create a new
                            MRS cluster resource.
                          type: number
                        dataVolumeType:
                          description: 'Specifies the data disk flavor of the nodes.
                            Required if data_volume_count is greater than zero. Changing
                            this will create a new MRS cluster resource. The following
                            disk types are supported:'
                          type: string
                        flavor:
                          description: Specifies the instance specifications for each
                            nodes in node group. Changing this will create a new MRS
                            cluster resource.
                          type: string
                        nodeNumber:
                          description: Specifies the number of nodes for the node
                            group. Only the core group and task group updations are
                            allowed. The number of nodes after scaling cannot be less
                            than the number of nodes originally created.
                          type: number
                        rootVolumeSize:
                          description: Specifies the system disk size of the nodes.
                            Changing this will create a new MRS cluster resource.
                          type: number
                        rootVolumeType:
                          description: Specifies the system disk flavor of the nodes.
                            Changing this will create a new MRS cluster resource.
                          type: string
                      required:
                      - dataVolumeCount
                      - flavor
                      - nodeNumber
                      - rootVolumeSize
                      - rootVolumeType
                      type: object
                    type: array
                  streamingTaskNodes:
                    description: Specifies a list of the informations about the streaming
                      task nodes in the MRS cluster. The nodes object structure of
                      the streaming_task_nodes is documented below.
                    items:
                      properties:
                        assignedRoles:
                          description: Specifies the roles deployed in a node group.
                            This argument is mandatory when the cluster type is CUSTOM.
                            Each character string represents a role expression.
                          items:
                            type: string
                          type: array
                        dataVolumeCount:
                          description: 'Specifies the data disk number of the nodes.
                            The number configuration of each node are as follows:'
                          type: number
                        dataVolumeSize:
                          description: Specifies the data disk size of the nodes,in
                            GB. The value range is 10 to 32768. Required if data_volume_count
                            is greater than zero. Changing this will create a new
                            MRS cluster resource.
                          type: number
                        dataVolumeType:
                          description: 'Specifies the data disk flavor of the nodes.
                            Required if data_volume_count is greater than zero. Changing
                            this will create a new MRS cluster resource. The following
                            disk types are supported:'
                          type: string
                        flavor:
                          description: Specifies the instance specifications for each
                            nodes in node group. Changing this will create a new MRS
                            cluster resource.
                          type: string
                        nodeNumber:
                          description: Specifies the number of nodes for the node
                            group. Only the core group and task group updations are
                            allowed. The number of nodes after scaling cannot be less
                            than the number of nodes originally created.
                          type: number
                        rootVolumeSize:
                          description: Specifies the system disk size of the nodes.
                            Changing this will create a new MRS cluster resource.
                          type: number
                        rootVolumeType:
                          description: Specifies the system disk flavor of the nodes.
                            Changing this will create a new MRS cluster resource.
                          type: string
                      required:
                      - dataVolumeCount
                      - flavor
                      - nodeNumber
                      - rootVolumeSize
                      - rootVolumeType
                      type: object
                    type: array
                  subnetId:
                    description: Specifies the ID of the VPC Subnet which bound to
                      the MRS cluster. Changing this will create a new MRS cluster
                      resource.
                    type: string
                  subnetIdRef:
                    description: Reference to a VPCSubnet in vpc to populate subnetId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  subnetIdSelector:
                    description: Selector for a VPCSubnet in vpc to populate subnetId.
                    properties:
                      matchControllerRef:
                        description: MatchControllerRef ensures an object with the
                          same controller reference as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  tags:
                    additionalProperties:
                      type: string
                    description: Specifies the key/value pairs to associate with the
                      cluster.
                    type: object
                  templateId:
                    description: Specifies the template used for node deployment when
                      the cluster type is CUSTOM.
                    type: string
                  type:
                    description: Specifies the type of the MRS cluster. The valid
                      values are ANALYSIS, STREAMING, MIXED and CUSTOM (supported
                      in MRS 3.x only), default to ANALYSIS. Changing this will create
                      a new MRS cluster resource.
                    type: string
                  version:
                    description: Specifies the MRS cluster version. Currently, MRS
                      1.8.9, MRS 2.0.1, and MRS 3.1.0-LTS.1 are supported. Changing
                      this will create a new MRS cluster resource.
                    type: string
                  vpcId:
                    description: Specifies the ID of the VPC which bound to the MRS
                      cluster. Changing this will create a new MRS cluster resource.
                    type: string
                  vpcIdRef:
                    description: Reference to a VPC in vpc to populate vpcId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  vpcIdSelector:
                    description: Selector for a VPC in vpc to populate vpcId.
                    properties:
                      matchControllerRef:
                        description: MatchControllerRef ensures an object with the
                          same controller reference as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                required:
                - availabilityZone
                - componentList
                - managerAdminPwdSecretRef
                - masterNodes
                - name
                - nodeKeyPairSecretRef
                - version
                type: object
              providerConfigRef:
                default:
                  name: default
                description: ProviderConfigReference specifies how the provider that
                  will be used to create, observe, update, and delete this managed
                  resource should be configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: Resolution specifies whether resolution of this
                          reference is required. The default is 'Required', which
                          means the reconcile will fail if the reference cannot be
                          resolved. 'Optional' means this reference will be a no-op
                          if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: Resolve specifies when this reference should
                          be resolved. The default is 'IfNotPresent', which will attempt
                          to resolve the reference only when the corresponding field
                          is not present. Use 'Always' to resolve the reference on
                          every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              providerRef:
                description: 'ProviderReference specifies the provider that will be
                  used to create, observe, update, and delete this managed resource.
                  Deprecated: Please use ProviderConfigReference, i.e. `providerConfigRef`'
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: Resolution specifies whether resolution of this
                          reference is required. The default is 'Required', which
                          means the reconcile will fail if the reference cannot be
                          resolved. 'Optional' means this reference will be a no-op
                          if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: Resolve specifies when this reference should
                          be resolved. The default is 'IfNotPresent', which will attempt
                          to resolve the reference only when the corresponding field
                          is not present. Use 'Always' to resolve the reference on
                          every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: PublishConnectionDetailsTo specifies the connection secret
                  config which contains a name, metadata and a reference to secret
                  store config to which any connection details for this managed resource
                  should be written. Connection details frequently include the endpoint,
                  username, and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: SecretStoreConfigRef specifies which secret store
                      config should be used for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: Resolution specifies whether resolution of
                              this reference is required. The default is 'Required',
                              which means the reconcile will fail if the reference
                              cannot be resolved. 'Optional' means this reference
                              will be a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: Resolve specifies when this reference should
                              be resolved. The default is 'IfNotPresent', which will
                              attempt to resolve the reference only when the corresponding
                              field is not present. Use 'Always' to resolve the reference
                              on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations are the annotations to be added to
                          connection secret. - For Kubernetes secrets, this will be
                          used as "metadata.annotations". - It is up to Secret Store
                          implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels are the labels/tags to be added to connection
                          secret. - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store
                          types.
                        type: object
                      type:
                        description: Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: WriteConnectionSecretToReference specifies the namespace
                  and name of a Secret to which any connection details for this managed
                  resource should be written. Connection details frequently include
                  the endpoint, username, and password required to connect to the
                  managed resource. This field is planned to be replaced in a future
                  release in favor of PublishConnectionDetailsTo. Currently, both
                  could be set independently and connection details would be published
                  to both without affecting each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: ClusterStatus defines the observed state of Cluster.
            properties:
              atProvider:
                properties:
                  analysisCoreNodes:
                    description: Specifies a list of the informations about the analysis
                      core nodes in the MRS cluster. The nodes object structure of
                      the analysis_core_nodes is documented below.
                    items:
                      properties:
                        hostIps:
                          description: The host list of this nodes group in the cluster.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  analysisTaskNodes:
                    description: Specifies a list of the informations about the analysis
                      task nodes in the MRS cluster. The nodes object structure of
                      the analysis_task_nodes is documented below.
                    items:
                      properties:
                        hostIps:
                          description: The host list of this nodes group in the cluster.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  chargingStartTime:
                    description: The charging start time which is the start time of
                      billing, in RFC-3339 format.
                    type: string
                  components:
                    items:
                      properties:
                        description:
                          description: Component description.
                          type: string
                        id:
                          description: The cluster ID in UUID format.
                          type: string
                        name:
                          description: Specifies the name of the MRS cluster. The
                            name can contain 2 to 64 characters, which may consist
                            of letters, digits, underscores (_) and hyphens (-). Changing
                            this will create a new MRS cluster resource.
                          type: string
                        version:
                          description: Specifies the MRS cluster version. Currently,
                            MRS 1.8.9, MRS 2.0.1, and MRS 3.1.0-LTS.1 are supported.
                            Changing this will create a new MRS cluster resource.
                          type: string
                      type: object
                    type: array
                  createTime:
                    description: The cluster creation time, in RFC-3339 format.
                    type: string
                  customNodes:
                    description: Specifies a list of the informations about the custom
                      nodes in the MRS cluster. The nodes object structure of the
                      custom_nodes is documented below. Unlike other nodes, it needs
                      to specify group_name.
                    items:
                      properties:
                        hostIps:
                          description: The host list of this nodes group in the cluster.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  id:
                    description: The cluster ID in UUID format.
                    type: string
                  masterNodeIp:
                    description: The IP address of the master node.
                    type: string
                  masterNodes:
                    description: Specifies a list of the informations about the master
                      nodes in the MRS cluster. The nodes object structure of the
                      master_nodes is documented below. Changing this will create
                      a new MRS cluster resource.
                    items:
                      properties:
                        hostIps:
                          description: The host list of this nodes group in the cluster.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  privateIp:
                    description: The preferred private IP address of the master node.
                    type: string
                  status:
                    description: 'The cluster state, which include: running, frozen,
                      abnormal and failed.'
                    type: string
                  streamingCoreNodes:
                    description: Specifies a list of the informations about the streaming
                      core nodes in the MRS cluster. The nodes object structure of
                      the streaming_core_nodes is documented below.
                    items:
                      properties:
                        hostIps:
                          description: The host list of this nodes group in the cluster.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  streamingTaskNodes:
                    description: Specifies a list of the informations about the streaming
                      task nodes in the MRS cluster. The nodes object structure of
                      the streaming_task_nodes is documented below.
                    items:
                      properties:
                        hostIps:
                          description: The host list of this nodes group in the cluster.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  totalNodeNumber:
                    description: The total number of nodes deployed in the cluster.
                    type: number
                  updateTime:
                    description: The cluster update time, in RFC-3339 format.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time this condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: A Message containing details about this condition's
                        last transition from one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: Type of this condition. At most one of each condition
                        type may apply to a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
